/** @type {import('next').NextConfig} */
const nextConfig = {
    sassOptions: {
      includePaths: ['./src'],
    },
    // Add any other required configurations
  };
  
  export default nextConfig;